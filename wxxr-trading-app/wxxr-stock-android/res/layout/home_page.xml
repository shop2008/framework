<com.wxxr.mobile.stock.client.widget.SliderLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://wxxr.com.cn/binding"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    bind:menuAdaptor=".widget.HomePageMenuAdaptor">

    <!-- As the main content view, the view below consumes the entire
         space available using match_parent in both dimensions. -->
    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        bind:field="contents"
        android:background="@color/white"
        />

    <!-- android:layout_gravity="start" tells DrawerLayout to treat
         this as a sliding drawer on the left side for left-to-right
         languages and on the right side for right-to-left languages.
         The drawer is given a fixed width in dp and extends the full height of
         the container. A solid background is used for contrast
         with the content view. -->
    <ListView
        android:id="@+id/left_drawer"
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:dividerHeight="0dp"
        android:divider="@null"
        android:scrollbars="none"
        android:fadingEdge="none"
        android:background="@drawable/left_menu_bj"
        bind:field="leftMenu"
        bind:itemViewId="mainMenuItemView"
        android:cacheColorHint="#00000000"
        bind:on_ItemClick="handleItemClick"
        />
    <ListView
        android:id="@+id/right_drawer"
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:choiceMode="singleChoice"
        android:dividerHeight="0dp"
        android:divider="@null"
        android:scrollbars="none"
        android:fadingEdge="none"
        android:background="@color/slideMenu_l_r_background"
        bind:field="rightMenu"
        bind:itemViewId="mainMenuItemView"
        android:cacheColorHint="#00000000"
        bind:on_ItemClick="handleItemClick"
        />
</com.wxxr.mobile.stock.client.widget.SliderLayout>
