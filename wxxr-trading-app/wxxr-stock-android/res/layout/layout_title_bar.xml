<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    style="@style/style_title_bar_bg"
    >
    
    <ImageButton 
        android:id="@id/ib_toggle_left_menu"
        android:contentDescription="@null"
        style="@style/style_ib_left"
        />

    
    <TextView 
        android:id="@id/tv_title_content"
        style="@style/style_title_text"
        />

    <ImageButton
        android:id="@id/ib_toggle_right_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@null"
        android:src="@drawable/ic_title_bar_personal" 
        android:contentDescription="@null"
        />

    <ImageButton
        android:id="@id/ib_toggle_search"
        android:layout_marginRight="10dip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/ib_toggle_right_menu"
        android:layout_toLeftOf="@id/ib_toggle_right_menu"
        android:background="@null"
        android:src="@drawable/ic_title_bar_search" 
        android:contentDescription="@null"
        />

</RelativeLayout>